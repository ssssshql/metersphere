## 一、下载最新离线安装包

<https://metersphere.io/docs/v3.x/installation/offline_installation/>

## 二、GitHub 最新代码同步至内网 GitLab（v3.x 分支）

### 前提条件


1. 本地已克隆内网 GitLab 的 MeterSphere 仓库（v3.x 分支），且能正常提交 / 推送代码；
2. 本地已配置 GitHub 的 MeterSphere 仓库作为远程仓库（命名为`github`）；
3. 确保本地代码工作区干净（无未提交的修改），可通过`git status`验证。

### 操作步骤

#### 步骤 1：配置 GitHub 远程仓库（首次操作）

```bash
# 进入本地GitLab仓库目录
cd /opt/code/metersphere

# 添加GitHub远程仓库（命名为github，方便区分）
git remote add github https://github.com/metersphere/metersphere.git

# 验证远程仓库配置
git remote -v
# 预期输出：
# github  https://github.com/metersphere/metersphere.git (fetch)
# github  https://github.com/metersphere/metersphere.git (push)
# origin  内网GitLab地址 (fetch)
# origin  内网GitLab地址 (push)
```

#### 步骤 2：拉取 GitHub v3.x 分支最新代码

```bash
# 拉取GitHub远程仓库所有分支的最新代码（仅拉取，不合并）
git fetch github

# 切换到本地v3.x分支（确保当前分支是v3.x）
git checkout v3.x

# 合并GitHub v3.x分支的最新代码到本地v3.x
git merge github/v3.x
```

#### 步骤 3：解决合并冲突（若有）

合并过程中若出现冲突，Git 会提示冲突文件，此时需：


1. 打开冲突文件，找到`<<<<<<< HEAD`（本地 GitLab 代码）、`=======`（GitHub 新代码）、`>>>>>>> github/v3.x`标记的冲突区域；
2. 根据业务需求保留 / 整合代码（保留内网自定义逻辑，同时合并 GitHub 的官方更新）；
3. 解决完所有冲突后，标记文件为已解决：

   ```bash
   git add <冲突文件路径>
   ```
4. 完成合并提交：

   ```bash
   git commit -m "merge: 同步GitHub v3.x最新代码，解决xxx文件冲突"
   ```

#### 步骤 4：推送合并后的代码到内网 GitLab

```bash
git push origin v3.x
```


## 二、docker构建最新镜像

在项目源码目录构建docker镜像

```bash
docker build --no-cache -f Dockerfile.buildjar -t metersphere:3.x .
```

## 三、修改docker-compose配置文件

在 /opt/metersphere 目录修改 docker-compose-metersphere.yaml文件

```bash
version: "3"
services:
  metersphere:
# 修改镜像名为我们构建的镜像 
    image: metersphere:3.x
    container_name: metersphere
    deploy:
      resources:
        limits:
          memory: ${MS_MEM_LIMIT}
# 删除或者注释这一行
#    entrypoint:
#      - sh
#      - -c
#      - |
#        sh /shells/metersphere.sh
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://${MS_MYSQL_HOST}:${MS_MYSQL_PORT}/${MS_MYSQL_DB}?autoReconnect=false&useUnicode=true&characterEncoding=UTF-8&characterSetResults=UTF-8&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true&useSSL=false&sessionVariables=sql_mode=%27STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION%27
      SPRING_DATASOURCE_USERNAME: ${MS_MYSQL_USER}
      SPRING_DATASOURCE_PASSWORD: ${MS_MYSQL_PASSWORD}
      KAFKA_BOOTSTRAP_SERVERS: ${MS_KAFKA_HOST}:${MS_KAFKA_PORT}
      K8S_TASK_RUNNER_IMAGE: ${MS_IMAGE_PREFIX}/task-runner:${MS_IMAGE_TAG}
      SPRING_SESSION_TIMEOUT: '30d'
      REDIS_HOST: ${MS_REDIS_HOST}
      REDIS_PORT: ${MS_REDIS_PORT}
      REDIS_PASSWORD: ${MS_REDIS_PASSWORD}
      MINIO_ENDPOINT: ${MS_MINIO_ENDPOINT}
      MINIO_ACCESS_KEY: ${MS_MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MS_MINIO_SECRET_KEY}
      TOTP_SECRET: ${MS_TOTP_SECRET}
    ports:
      - ${MS_SERVER_PORT}:8081
    healthcheck:
      test: [ "CMD", "bash", "-c", "< /dev/tcp/localhost/8081" ]
      interval: 6s
      timeout: 10s
      retries: 50
    restart: always
    volumes:
      - ${MS_BASE}/metersphere/conf:/opt/metersphere/conf
      - ${MS_BASE}/metersphere/logs/metersphere:/opt/metersphere/logs/metersphere
    networks:
      - ms-network
```

## 四、使用msctl重载配置

```bash
msctl reload
```


