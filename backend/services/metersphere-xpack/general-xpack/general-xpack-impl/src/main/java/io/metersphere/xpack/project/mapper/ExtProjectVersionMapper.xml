<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.metersphere.xpack.project.mapper.ExtProjectVersionMapper">
    <select id="list" resultType="io.metersphere.xpack.project.dto.ProjectVersionDTO">
        select id, project_id as projectId, name, description, if(status = 'open', true, false) as `status`, latest, publish_time publishTime, create_time createTime, create_user createUser from project_version
        where project_id = #{request.projectId}
        <if test="request.keyword != null and request.keyword != ''">
            and name like concat('%', #{request.keyword}, '%')
        </if>
    </select>
</mapper>